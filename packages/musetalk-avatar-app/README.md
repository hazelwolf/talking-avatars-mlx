# musetalk-avatar-app

Minimal Gradio interface for the MuseTalk MLX video-driven talking avatar.

Wraps `musetalk-avatar-core`. Run it with `uv run musetalk-avatar` (or
`uv run --package musetalk-avatar-app python -m musetalk_avatar_app.app`), then open
http://127.0.0.1:7860.
